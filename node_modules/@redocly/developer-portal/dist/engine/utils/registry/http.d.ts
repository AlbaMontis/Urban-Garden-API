export function downloadContentsWithRetries(link: any, headers: any, { retries, reporter }: {
    retries?: number;
    reporter: any;
}): Promise<any>;
/**
 *
 * @param {{
 *   link: string,
 *   headers: Record<string, string> ,
 *   reporter: import('gatsby').Reporter,
 *   fileAbsolutePath: string,
 *   downloadOnlyOpenapi: boolean,
 *   contentDir: string
 * }} downloadAssetOptions
 * @returns {Promise<void | any[]>}
 */
export function downloadFileWithRetries(downloadAssetOptions: {
    link: string;
    headers: Record<string, string>;
    reporter: import('gatsby').Reporter;
    fileAbsolutePath: string;
    downloadOnlyOpenapi: boolean;
    contentDir: string;
}): Promise<void | any[]>;

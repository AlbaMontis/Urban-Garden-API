export function retry(fn: any, { reporter, link, interval, retries, currentAttempt }: {
    reporter: any;
    link: any;
    interval?: number;
    retries?: number;
    currentAttempt?: number;
}): Promise<any>;

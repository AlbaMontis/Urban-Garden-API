import{slash}from"@redocly/openapi-core";import{useStaticQuery,graphql}from"gatsby";import*as React from"react";import{joinWithSeparator}from"../../helpers";import styled from"styled-components";export default function EditPageButton(a){const{editPageOverride:b,pageId:c}=a,{siteConfig:{editPage:d}}=useStaticQuery(graphql`
    query EditPageQuery {
      siteConfig {
        editPage {
          baseUrl
          icon
          text
        }
      }
    }
  `),e=b?.baseUrl||d?.baseUrl,f=e&&c?slash(joinWithSeparator(e,c)):null;if(null==f||!0===b?.disable)return null;const g=b?.icon||d.icon,h=b?.text||d.text;return React.createElement(EditPageButtonWrapper,{href:f,target:"_blank"},null!=g&&React.createElement(EditPageIcon,{src:g}),h?h:"Edit")}const EditPageIcon=styled.img`
  height: 0.9em;
  margin: 3px;
`,EditPageButtonWrapper=styled.a`
  display: flex;
  color: inherit;
  text-decoration: none;
  align-items: end;
  font-size: 1.2em;
  font-weight: ${({theme:a})=>a.typography.fontWeightBold};
  margin-left: auto;
`;